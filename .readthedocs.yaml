# .readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.13"

sphinx:
  configuration: backend/docs/conf.py

python:
  install:
    - method: pip
      path: backend
      extra_requirements:
        - docs